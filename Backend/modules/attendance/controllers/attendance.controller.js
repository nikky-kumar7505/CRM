// ─── PLACEHOLDER ─────────────────────────────────────────────────────────────
// Attendance module will be built later
// Routes are registered so backend is compatible

export const getAttendance = async (req, res) => {
    res.status(200).json({
      success: true,
      message: "Attendance module coming soon.",
      data: [],
    });
  };
  
  export const markAttendance = async (req, res) => {
    res.status(200).json({
      success: true,
      message: "Attendance module coming soon.",
    });
  };